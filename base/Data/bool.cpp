#include<iostream>
using namespace std;

int main() {
    bool is_ready = true;
    int ans = true;     //ans assigned 1
    int promise = false;    //promise assigned 0
    bool start = -100;  //start assigned true
    bool stop = 0;      //stop assigned false
    cout << is_ready << ans << promise << start << stop << endl;
    return 0;
}
